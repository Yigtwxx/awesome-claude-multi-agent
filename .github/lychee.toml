# Lychee link checker configuration
# https://lychee.cli.rs/configuration/

# Accept 403 status codes — some sites (Reddit, DeepLearning.AI) block bots
accept = [200, 203, 403]

# Exclude URLs known to block automated requests
exclude = [
  "reddit\\.com",
  "learn\\.deeplearning\\.ai"
]
